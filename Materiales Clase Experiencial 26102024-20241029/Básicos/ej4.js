// Define una función llamada "sumar" que toma dos parámetros "a" y "b" (ambos números)
function sumar(a, b) {
    // Retorna la suma de "a" y "b"
    return a + b;
}

// Llama a la función con dos números y muestra el resultado en la consola
console.log(sumar(3, 7)); // Salida: 10
